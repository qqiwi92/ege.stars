import { P } from "@/components/ui/typography";
import { PracticeLesson } from "@/lib/types/Lesson";

export default function Practice({
  lessonData,
}: {
  lessonData: PracticeLesson;
}) {
  return <P>practice</P>;
}
